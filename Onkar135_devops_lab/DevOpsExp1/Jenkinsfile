pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/yourusername/your-sample-repo.git'
            }
        }

        stage('Build Docker Image') {
            steps {
                script {
                    sh 'docker build -t simple-app ./simple-app'
                }
            }
        }

        stage('Run App Container') {
            steps {
                script {
                    sh 'docker run --rm simple-app'
                }
            }
        }
    }
}
