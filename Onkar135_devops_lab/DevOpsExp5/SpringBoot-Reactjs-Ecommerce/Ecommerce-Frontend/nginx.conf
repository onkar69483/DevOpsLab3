server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        # Handles both real files and SPA routes
        try_files $uri $uri/ /index.html;
    }

    # No need for error_page 404 override
}
